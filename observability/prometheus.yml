global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "barbersuite-backend"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - "barbersuite-backend:8080"
